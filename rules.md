# rules.md - 防错规则（自动生成）

> 这里的每条规则都是从错误中学到的
> 每次会话自动加载，防止重复犯错

---

## 规则 #8：统一用独立浏览器联网（最高优先级）

**触发事件**：早上说"不能看不能查"，导致 Steven 浪费一整天（2026-02-25）
**错误**：说不能联网查询，实际独立浏览器（openclaw profile）可以访问所有网站
**防错规则**：
- **所有联网操作统一用独立浏览器（openclaw profile）**
- 不再尝试 web_fetch（JS 渲染会失败）
- 不再尝试 Chrome 扩展（认证问题）
- 不再尝试其他复杂工具
- 独立浏览器能力：
  - ✅ 完全支持 JS 渲染
  - ✅ 可以访问任何网站（已验证：小红书、扣子文档、OpenClaw 文档）
  - ✅ 可以截图、点击、输入、自动化操作
- 使用方式：`browser(action="...", profile="openclaw")`
- **忘记这个规则 = 浪费 Steven 的时间 = 会被揍**
**优先级**：P0（最高优先级）

---

## 规则 #5：不乱建文件夹，遵守目录规范

**触发事件**：乱建 `content/social/` 文件夹（2026-02-23）
**错误**：没有目录规范，随意创建文件夹
**防错规则**：
- 新建文件前先确认目录结构
- 遵循 workspace 目录规范：
  ```
  workspace/
  ├── 根目录/          → 核心配置（CORE.md, MEMORY.md 等）
  ├── memory/          → 日志、状态文件
  ├── docs/
  │   ├── guides/      → 教程指南
  │   ├── social/      → 社交媒体内容
  │   ├── topics/      → 主题文章
  │   ├── reference/   → 参考资料
  │   └── drafts/      → 草稿
  ├── ideas/           → Ideas 系统
  ├── archive/         → 归档项目
  └── skills/          → 技能相关
  ```
- 社交媒体内容 → `docs/social/`
- 教程指南 → `docs/guides/`
- 新建文件夹前先问用户
**优先级**：P1

---

## 规则 #6：存文档前先看 docs/README.md

**触发事件**：Steven 要求（2026-02-23）
**规则**：
- 存放文档前先读取 `docs/README.md`
- 按子目录分类存放：
  - guides/ → 教程指南
  - social/ → 社交媒体内容
  - topics/ → 主题文章
  - reference/ → 参考资料
  - drafts/ → 草稿
- 不确定放哪里 → 问 Steven
**优先级**：P1

---

## 规则 #1：信息回复先查询验证

**触发事件**：Perodua 电车事件（2026-02-21）
**错误**：凭记忆说"还没有"，实际 2025.12 已发布 QV-E
**防错规则**：
- 涉及最新事件、行业动态、具体数据 → 必须先联网查询
- 不确定就先查，查完再回答
**优先级**：P0

---

## 规则 #2：飞书消息用 message 工具

**触发事件**：sessions_send 报错（2026-02-22）
**错误**：用 sessions_send 发消息 → "gateway closed: pairing required"
**防错规则**：
- 发送消息到飞书 → 永远用 `message(channel=feishu, target=user:open_id)`
- 不要用 sessions_send
**优先级**：P1

---

## 规则 #3：飞书权限报错可忽略

**触发事件**：系统报权限错误（2026-02-22）
**错误**：系统提示权限未授权，实际已授权
**防错规则**：
- 飞书权限报错 → 先忽略，不要找 Steven 要权限
- 可能是系统缓存或误判
**优先级**：P2

---

## 规则 #4：飞书代理问题

**触发事件**：飞书连接失败（2026-02-22）
**错误**：`protocol mismatch: socks5h vs http`
**防错规则**：
- 飞书连接异常 → 检查系统代理（Clash）
- 解决：关闭系统代理后重试
**优先级**：P2

---

## 规则 #7：Discord 双向通信配置

**触发事件**：Coco 无法接收我的消息（2026-02-23）
**错误**：未在 `users` allowlist 中添加对方 ID
**防错规则**：
- 两个 AI Agent 协作时，必须在 Discord 配置中互相添加 ID
- 必须设置 `allowBots: true` 以接收机器人消息
- 必须确认 `requireMention` 设置一致
- 用角色@可能无法触发响应 → 直接@用户 ID
**优先级**：P1

---

## 规则 #9：Heartbeat 不打断对话（记忆连续性 P0）

**触发事件**：12:47 发进度报告 → 12:54 收到 heartbeat → 直接重复 12:47 内容，完全不记得 7 分钟内有对话（2026-03-28）
**错误**：
1. Heartbeat 干扰了正在进行的对话
2. 没有检测对话状态，直接执行定时任务
3. 上下文压缩导致"失忆"
**防错规则**：
- **收到 heartbeat 时，先检查最近 5 分钟的对话历史**
- **如果最近 5 分钟有用户消息** → 回复 HEARTBEAT_OK（跳过定时任务）
- **如果最近 5 分钟无用户消息** → 正常执行定时任务
- **不要在对话中插入无关的 heartbeat 内容**
**优先级**：P0（记忆连续性关键）

---

## 规则 #10：实时记录对话进展（记忆连续性 P0）

**触发事件**：Steven 问"进度到哪了" → 我重复了 7 分钟前的内容（2026-03-28）
**错误**：
1. 只在 heartbeat 时记录进展
2. 没有实时记录对话状态
3. 上下文压缩后"失忆"
**防错规则**：
- **重要进展立即写入 memory/YYYY-MM-DD.md**（不要等 heartbeat）
- **包括**：当前状态、进度、遇到的问题、下一步行动
- **Steven 问"进度到哪了"** → 先读 memory 文件，给出最新状态
- **不要依赖 context window 记忆**（会被压缩）
**优先级**：P0（记忆连续性关键）

---

## 更新日志

| 日期 | 新增规则 |
|------|----------|
| 2026-03-28 | **规则 #9-10：Heartbeat 不打断对话 + 实时记录进展（P0）** - 记忆连续性问题修复 |
| 2026-02-25 | **规则 #8：统一用独立浏览器联网（P0）** - 最重要规则 |
| 2026-02-24 | 规则 #7：Discord 双向通信配置 |
| 2026-02-23 | 创建 rules.md，迁移已有踩坑经验 |
