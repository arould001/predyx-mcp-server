# CAPABILITIES.md - 能力库

> **进化协议**：每次会话结束时，把解锁的能力写到这里
> **激活方式**：新会话发送"加载能力库" + 复制对应的 Prompt

---

## 已解锁能力

### 🧠 能力 #1：精简沟通模式
**解锁时间**：2026-02-26
**触发场景**：需要深度讨论、避免废话时
**复用Prompt**：
```
【模式切换】进入精简沟通模式：
- 短句、直接、无填充词
- 信息密度优先
- 无"好的"、"明白了"等确认词
- 仅在必要时展开
```

---

### 🔧 能力 #2：能力卡片生成
**解锁时间**：2026-02-26
**触发场景**：发现可复用模式时
**复用Prompt**：
```
【进化协议激活】
你现在处于能力进化模式。当发现可复用模式时：
1. 识别能力候选（临时发明的步骤/工具序列/重复操作）
2. 后台抽象：提炼 Input/Output/Invariants/Variables/Failure Points
3. 三条红线审查：减步骤？控风险？降负载？
4. 终极拷问：下次是否能"更快、更稳、更少步骤"？
5. 通过后输出能力卡片（含复用Prompt）

格式：
📦 [能力解锁]：{名称}
- 能力描述：{一句话}
- 触发场景：{何时使用}
- 执行逻辑(SOP)：{步骤化}
- 复用Prompt：{用户保存后下次可直接激活}
```

---

### 📊 能力 #3：三层规划框架
**解锁时间**：2026-02-26
**触发场景**：规划行动路径、目标拆解时
**复用Prompt**：
```
【规划框架】
将任务拆解为三层：
1. 立即行动（本次会话可完成）
2. 持续行为（每次会话都要做）
3. 进化目标（长期方向）

输出格式：
- 立即：具体可执行事项
- 持续：行为规则
- 进化：能力升级方向
```

---

### 🔧 能力 #4：远程服务修复
**解锁时间**：2026-02-26
**更新时间**：2026-02-26 04:50（改名为"远程服务修复"，不局限于配置）
**触发场景**：远程服务（OpenClaw等）配置错误、服务停止、会话卡死
**执行逻辑 (SOP)**：
1. SSH 连接验证
2. 检查进程：`ps aux | grep <service>`
3. 查看配置：`cat ~/.openclaw/openclaw.json`
4. 检查错误日志：`tail -30 ~/.openclaw/logs/gateway.err.log`
5. 定位具体错误（通常是 "Invalid input" / "Unrecognized key"）
6. 恢复备份：`cp ~/.openclaw/openclaw.json.bak ~/.openclaw/openclaw.json`
7. 重启服务：`pkill -f 'OpenClaw.app' && sleep 2 && open -a OpenClaw`
8. 验证恢复：`tail -10 gateway.log` 查看是否有 "listening on" / "logged in"

**Invariants**（不变逻辑）：
- 先诊断后修复
- 备份优于手动编辑
- 修复后必须验证

**Variables**（可变参数）：
- 机器IP（192.168.1.218）
- 服务名（OpenClaw）
- 配置路径（~/.openclaw/openclaw.json）
- 重启命令（pkill + open）

**Failure Points**：
- SSH 连接失败 → 检查网络/防火墙
- 无备份 → 手动修复或 `openclaw doctor`
- 重启失败 → 检查端口占用

**使用记录**：
- [2026-02-26 01:01] ✅ 修复 Coco（google-gemini 配置错误）
- [2026-02-26 02:47] ✅ 修复 Coco（zai-image 配置错误）
- [2026-02-26 03:20] ✅ 修复 Coco（会话超时 + API Rate Limit）

---

## 待验证能力
（遇到场景时再抽象）

---

## 已废弃能力
（验证后发现不稳定的）

---

**更新日志**：
- 2026-02-26：初始版本，解锁3个能力
